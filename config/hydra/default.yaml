run:
  dir: ${paths.log_dir}/${experiment_name}/seed_${seed}/${now:%Y-%m-%d_%H-%M-%S}

sweep:
  dir: ${paths.log_dir}/multiruns/${now:%Y-%m-%d_%H-%M-%S}
  subdir: ${hydra.job.num}

job_logging:
  handlers:
    file:
      filename: ${hydra.runtime.output_dir}/${task_name}.log
